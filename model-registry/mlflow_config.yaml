# MLflow Configuration
# This file contains MLflow server configuration

tracking_uri: postgresql://postgres:password@db:5432/mlflow
backend_store_uri: postgresql://postgres:password@db:5432/mlflow
default_artifact_root: /mlflow/artifacts

# Experiment settings
experiments:
  default_name: "Default"
  
# Server settings
server:
  host: 0.0.0.0
  port: 5000
  workers: 4
